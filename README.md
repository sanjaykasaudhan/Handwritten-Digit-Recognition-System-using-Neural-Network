# Handwritten-Digit-Recognition-System-using-Neural-Network
The project uses the MNIST dataset, which is a standard benchmark in machine learning for recognizing handwritten digits (0-9). The notebook demonstrates a complete machine learning pipeline from data loading to model training and evaluation.
